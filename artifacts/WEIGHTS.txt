SMOKE WEIGHTS (interim) -- to be replaced by scripts/train.py after feature extraction.

feature_variant=pre  (pre=768 CLIP + 28 forensic = 796-D; proj=512+28=540-D)
calibration=none
Fit on 2 sample fixtures only (scripts/smoke_train.py). NOT a real classifier.

Real weights: run
  python scripts/extract_features.py --split train --augment
  python scripts/extract_features.py --split val
  python scripts/train.py
which A/B-selects pre vs proj by GroupKFold(5) CV AUC, sigmoid-calibrates on a
group-disjoint 30% val slice, and reports metrics on the held-out 70% test slice.

Encoder: frozen openai/clip-vit-base-patch32 (ViT-B/32, well under 2B).
Labels: SID-Set 0=real, 1=AIGC-positive (includes tampered/label 2). WildFake unused.
